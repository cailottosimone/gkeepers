# Changelog — Repository Esercizi Portieri

Le voci più recenti sono in cima; lo storico precedente resta sotto e non viene mai cancellato.
Formato pacchetto di consegna: `repository-portieri-v{X.Y}-{progressivo}.zip` (progressivo univoco, mai sovrascritto).

## snella-v1.0-001 — 2026-08-17 (RAMO STACCATO da v2.4-001)
Refactor di semplificazione richiesto esplicitamente come **ramo di sviluppo separato**:
il ramo `v2.4-xxx` originale resta intatto e recuperabile, questo pacchetto (`snella-v1.0-xxx`)
prosegue autonomamente. Schema JSON `SCHEMA_VERSION` 2.3 → **2.4** (tutte le aggiunte sotto
sono additive: import di backup più vecchi resta pienamente compatibile).

- **Modalità app Semplice/Completa** (`profile.appMode`, default "semplice"): nuova card in
  Impostazioni con selettore. In modalità Semplice, Presenze e Report escono dalla barra di
  navigazione e diventano irraggiungibili anche via route diretta (`setRoute` le reindirizza);
  in modalità Completa tornano visibili identiche a prima. **Nessun file cancellato, nessuna
  logica rimossa**: è un filtro di visualizzazione, reversibile in qualunque momento senza
  perdita di dati, pensato anche in vista di un'eventuale distinzione futura tra edizione
  gratuita/base e edizione completa a pagamento.
- **Tag di stato salute portiere** (`goalkeeper.healthStatus`: In salute / Infortunato / In
  recupero, default "healthy"): badge colorato sempre visibile su card portiere, scheda
  dettaglio e form di modifica. Resta distinto dalle note mediche esistenti (tags + testo
  libero), che continuano a fare da diario dettagliato.
- **Sedute a blocchi liberi** (`session.blocks: [{id, title, notes, exerciseIds}]`): la seduta
  non è più un elenco piatto di soli esercizi da catalogo, ma una sequenza di blocchi liberi
  (titolo + note libere), con esercizi dal catalogo agganciabili in modo **opzionale** per
  blocco. Composer riscritto: un blocco alla volta è "attivo" per l'aggiunta di esercizi
  (stesso selettore con filtri di prima, ora scoped al blocco), riordino su/giù, eliminazione
  blocco. **Migrazione automatica non distruttiva**: le sedute esistenti (solo `exerciseIds`
  piatti) vengono avvolte in un blocco unico "Esercizi" al primo caricamento, sia da IndexedDB
  sia da import JSON. `session.exerciseIds`/`session.aggregated` restano in cima alla seduta
  come cache piatta derivata dai blocchi ad ogni salvataggio, per compatibilità totale con
  Report, Presenze e scheda portiere (storico sedute/esercizi), che continuano a leggerli senza
  alcuna modifica.
- **Tempi esercizi nascosti in modalità Semplice**: il blocco "Struttura dell'esercizio"
  (serie/ripetizioni/tempo di lavoro/recupero, anteprima durata) sparisce dal form di
  creazione/modifica esercizio, dalla card e dalla scheda dettaglio quando l'app è in modalità
  Semplice; identico a prima in modalità Completa. **Nessuna perdita di dati**: se un esercizio
  aveva già dei tempi (creato in modalità Completa), il salvataggio in modalità Semplice
  preserva i valori esistenti invece di azzerarli, perché il form semplificato non li legge né
  li sovrascrive.
- **Orari multipli in Stagione**: ogni collegamento seduta/evento su un giorno
  (`genericEvent.linkedItems[].time`, opzionale) può ora avere un orario libero. Il picker
  "Collega elemento" ha un campo orario facoltativo; gli elementi collegati in un giorno sono
  ordinati per orario con badge orario visibile (utile quando un giorno ha più sedute, es.
  mattina/pomeriggio). Nessuna modifica al modello "un giorno = un `GenericEvent`": è
  un'aggiunta additiva dentro `linkedItems`, che già supportava più collegamenti per giorno.
- **Passata usabilità mobile**: barra azioni sticky (pattern già in uso nell'editor esercizio)
  estesa al composer Sedute e al form Portiere; griglia calendario mensile più compatta sotto i
  460px (font/pillole/gap ridotti, min-height dimezzata) — la vista "Elenco" resta l'alternativa
  agenda-style già disponibile per chi preferisce una lista alla griglia; target di tocco
  aumentati per i controlli piccoli (riordino blocchi, rimozione chip) sotto i 620px.
- Verifica: `node --check` su tutti i file JS (incluso `js/data/*`, `js/components/*`), test di
  bilanciamento graffe CSS, module-graph test statico (import/export coerenti su tutti i moduli),
  test funzionali mirati su migrazione sedute legacy→blocchi, default `healthStatus`, e
  normalizzazione `linkedItems.time` — nessun ambiente browser reale disponibile in questa
  sessione per uno smoke test end-to-end: consigliata una verifica manuale rapida dopo
  l'apertura (vedi messaggio di consegna in chat per i punti da controllare).

## v2.4-001 — 2026-08-17
- **Sincronizzazione cloud (facoltativa), stesso sistema di Vacation Builder**: nuovo modulo `js/data/` (`config.js`, `auth.js`, `cloud.js`, `sync.js`) + `js/components/sync-indicator.js` + `supabase/schema.sql`/`supabase/README.md`. Login email+password via Supabase (stesso progetto condiviso della suite, schema Postgres dedicato `portieri`), pattern outbox (coda locale delle modifiche non ancora inviate) con drain ogni 5s e pull ogni 60s, conflitti risolti last-write-wins su `updatedAt` **autorevole lato server** (trigger Postgres, non l'orologio del dispositivo). Nuovo indicatore di stato nella barra di navigazione (Offline / Cloud non collegato / Da collegare / Sincronizzazione… / Sincronizzato / Errore, con badge del numero di modifiche in coda), cliccabile per aprire la nuova sezione **Impostazioni → Account e sincronizzazione** (login/registrazione, scelta esplicita "primo dispositivo → carica i dati locali" oppure "dispositivo successivo → scarica i dati dal cloud", disconnessione). Facoltativa in tutto e per tutto: senza account collegato l'app resta identica a prima, solo IndexedDB locale.
  - **Immagini** (allegati esercizio, foto portiere, logo profilo) caricate su Supabase Storage (bucket privato `portieri-immagini`) con **dedup per hash del contenuto**: la stessa immagine, anche riusata su record diversi o arrivata da un altro dispositivo, non viene mai ricaricata due volte. Adattamento rispetto a Vacation Builder: qui le immagini non sono sempre "array di stringhe data URL" (`exercise.attachments` è un array di oggetti `{type,name,dataUrl}`, `goalkeeper.photo`/`profile.logo` sono stringhe singole) — nuovo resolver generalizzato in `js/data/cloud.js` che gestisce entrambe le forme.
  - **Liste configurabili e profilo** (`customLists`, `profile`) sono un blob unico per utente, non righe separate: sincronizzati con logica LWW dedicata su una singola riga per (utente, chiave) nella tabella `settings`, non tramite l'outbox generico. **Il PIN del blocco app (`pinHash`/`appLock`) non lascia mai il dispositivo**, con o senza sincronizzazione attiva: stesso principio già in uso per l'export JSON (`stripProfileForExport`), riusato tale e quale anche per il payload cloud.
- **Prerequisito strutturale per il sync — soft delete + outbox in `storage.js`**: le eliminazioni (esercizi, sedute, portieri, stagioni, eventi, presenze, impegni pianificati, eventi specifici) non rimuovono più il record da IndexedDB ma lo marcano con `deletedAt` (tombstone), altrimenti un'eliminazione fatta su un dispositivo non potrebbe mai propagarsi agli altri. Le viste continuano a vedere solo i record vivi (stesso comportamento di prima): il filtro è automatico in tutte le `getAll*`/`get*`. `DB_VERSION` 3 → 4 (upgrade additivo, nessun dato esistente toccato). Ogni scrittura applicativa accoda ora una voce nella coda di sincronizzazione (`_outbox`), drenata dal motore di sync quando un account è collegato — senza account, la coda resta semplicemente inutilizzata (dimensione trascurabile).
- **Import backup completo: conferma con anteprima + scrittura atomica**: prima di sovrascrivere qualunque dato, un nuovo modale mostra i conteggi per tipo (esercizi, sedute, portieri, stagioni, impegni, eventi, presenze) ed eventuali avvisi, e richiede conferma esplicita — stesso principio già in uso per l'import configurazione. La scrittura passa ora da un'**unica transazione IndexedDB multi-store** (`storage.importAllAtomic`): se una singola voce fallisce (es. quota di archiviazione superata), l'intera transazione si annulla e **nessuno store resta modificato**, invece di un archivio parzialmente importato e incoerente tra i vari tipi di dato.
- **Fix — blocco PIN aggirabile con un click**: il link "Hai dimenticato il PIN?" sulla schermata di blocco puntava direttamente a `?resetlock=true`, disattivando il blocco a chiunque lo vedesse. Sostituito con un'istruzione testuale (non cliccabile): va letta e digitata a mano nell'indirizzo dell'app, non basta più un tap sulla lock screen stessa.
- **Compressione immagini lato client**: nuova `resizeImageFile` (`js/importExport.js`, canvas-based) applicata a allegati esercizio (max 1600px), foto portiere (max 800px) e logo profilo (max 400px) prima del salvataggio in IndexedDB — una foto da smartphone (spesso diversi MB) non finisce più intera in RAM/IndexedDB a ogni avvio, e pesa anche meno in eventuale upload cloud. Ricade sul file originale se già piccolo o non ridimensionabile (es. SVG): non blocca mai un caricamento.
- **Fix — focus nei modali**: `_openModal` ora sposta il focus dentro il modale all'apertura, lo intrappola (Tab/Shift+Tab non escono più verso lo sfondo) e lo restituisce all'elemento che l'ha aperto alla chiusura — problema di accessibilità da tastiera segnalato in una revisione interna, corretto insieme alle modifiche sopra perché il nuovo modale di conferma import lo rendeva più visibile.
- Nota: `schemaVersion` dello schema JSON (export/import, interoperabilità con l'artefatto Claude) resta **"2.3"**, invariato — nessuna di queste modifiche tocca la forma dei dati esportati, solo la persistenza locale (IndexedDB) e l'infrastruttura di sincronizzazione cloud, del tutto facoltativa.

## v2.3-025 — 2026-07-02
- **Fix dimensione punte frecce**: individuata la causa esatta — i `<marker>` introdotti per startCap/endCap usavano `markerUnits="strokeWidth"` con `markerWidth/markerHeight="9"`, che moltiplica la dimensione per lo stroke-width della linea (4.5), risultando in punte di **40.5 unità (9x lo spessore linea)** invece delle ~15 unità (3.3x, nel range 3-4x raccomandato) che la freccia aveva prima dell'introduzione di startCap/endCap. Passato a `markerUnits="userSpaceOnUse"` con dimensione assoluta fissa (15 unità base), indipendente dallo stroke-width della linea (che resta un controllo separato, non toccato). Nessuna modifica alla logica startCap/endCap (none/arrow/arrow_open) né allo stile della linea.
- **Nuovo campo `capScale`** (0.5–2.0, default 1.0) su ogni tipo di freccia: scala SOLO la geometria della punta (markerWidth/markerHeight del marker SVG), mai lo stroke-width della linea. Un unico valore per tipo/freccia si applica simmetricamente a entrambe le estremità (utile per frecce bidirezionali). Migrazione automatica: tipi/frecce già esistenti assumono `capScale:1` (la dimensione ripristinata) via fallback difensivo alla lettura, nessuna azione utente, nessuna riscrittura dei dati salvati.
- **UI Impostazioni**: nuovo campo numerico "Dimensione punta" (0.5–2.0, step 0.1) nel form "Tipi di freccia", accanto a Punta inizio/Punta fine; anteprima aggiornata per riflettere la scala scelta.
- **UI Editor, freccia libera**: stesso controllo nella toolbar freccia, disabilitato quando è selezionato un tipo (la dimensione deriva dal tipo, come già per colore/stile/punte) e attivo in modalità "Libero".
- Nota tecnica: `capScale` aggiunto agli stessi tre punti di normalizzazione `arrowTypes` in `importExport.js` già estesi nel giro precedente per startCap/endCap (export, import, merge), con lo stesso fallback difensivo — necessario per non perdere il nuovo campo ad ogni backup/ripristino; nessun'altra logica di import/export toccata.

## v2.3-024 — 2026-07-02
- **Editor SVG — controllo sulle punte delle frecce (startCap/endCap)**: ogni tipo di freccia (`customLists.arrowTypes`) ha ora due nuovi campi indipendenti, "Punta inizio linea" e "Punta fine linea", con tre valori possibili ciascuno: Nessuna, Freccia (piena), Freccia aperta (variante a "V", aggiunta perché a basso costo con l'approccio a marker SVG). Combinandoli si ottengono frecce solo-punta-finale (comportamento di prima), bidirezionali, o semplici linee senza punte per delimitazioni/collegamenti.
- **Migrazione automatica, nessuna azione richiesta**: i tipi di freccia già esistenti (creati prima di questa modifica) e le frecce libere già disegnate assumono implicitamente `startCap:"none", endCap:"arrow"` — il comportamento che avevano finora — tramite fallback difensivo alla lettura (stesso principio già in uso per colore/stile), senza bisogno di riscrivere alcun dato salvato né di alcun intervento dell'utente. `DEFAULT_ARROW_TYPES` aggiornato esplicitamente con gli stessi valori.
- **Rendering SVG a marker riutilizzabili**: sostituito il poligono disegnato a mano (sempre e solo in fondo alla linea) con veri `<marker>` SVG definiti nei `<defs>` del documento e collegati via `marker-start`/`marker-end`; un solo marker per ogni combinazione (tipo punta, colore) effettivamente usata nel documento — grazie a `orient="auto-start-reverse"` lo stesso marker si specchia automaticamente per l'estremità iniziale, senza doverlo duplicare. Aggiornati tutti e tre i punti di generazione SVG (editor live, `toSvgString`, `composeExerciseSvg`) senza toccare la logica di risoluzione già funzionante per colore/stile, solo estesa con i due campi aggiuntivi.
- **UI Impostazioni**: nel form "Tipi di freccia" due dropdown affiancati "Punta inizio"/"Punta fine" per ogni tipo (riga esistente e form di creazione), con anteprima aggiornata che mostra visivamente le punte scelte.
- **Editor, freccia libera**: gli stessi due controlli sono disponibili anche per le frecce senza tipo assegnato (colore/stile scelti manualmente), disabilitati quando è selezionato un tipo (le punte derivano dal tipo, come già avviene per colore/stile) e attivi in modalità "Libero".
- Nota tecnica: `startCap`/`endCap` sono stati aggiunti anche ai tre punti di normalizzazione arrowTypes in `importExport.js` (export, import, merge) con lo stesso fallback difensivo — modifica minima e mirata, necessaria per evitare che un backup/ripristino perdesse silenziosamente i nuovi campi; nessun'altra logica di import/export toccata.

## v2.3-023 — 2026-07-02
- **Report — Stagione e Periodo come filtri indipendenti**: rimossa la UI a tab/radio "Per Stagione"/"Periodo libero" (mutuamente esclusiva); ora due blocchi filtro sempre visibili e combinabili in AND — dropdown Stagione con default "Tutte le stagioni" (nessun filtro su quell'asse) e coppia data-dal/data-al opzionale (coppia affiancata, stesso stile già stabilito per i range date). Nessun blocco è obbligatorio: lasciandoli entrambi su default il report copre l'intero storico del portiere.
- **Pannelli filtri collassabili**, componente riutilizzabile unico (icona a imbuto + badge conteggio filtri attivi + pannello con animazione max-height), applicato in modo consistente con comportamento differenziato per sezione:
  - Categoria A (filtri sempre visibili, nessuna icona): Report e vista principale Presenze — invariati.
  - Categoria B primo livello (collassato di default, icona a imbuto accanto alla ricerca): Esercizi, Sedute, Portieri. Il badge numerico compare sull'icona solo quando ci sono filtri attivi.
  - Categoria B secondo livello: le sezioni accordion della scheda portiere (Storico Sedute, Storico Presenze, Esercizi effettuati) mantengono il primo livello di apertura invariato; al loro interno il pannello filtri è a sua volta collassato di default dietro la stessa icona a imbuto. L'elenco risultati è sempre visibile subito sotto l'icona, indipendentemente dall'apertura del pannello filtri interno.
  - Nota tecnica: la logica JS di questo pattern (markup del pulsante/badge, apertura/chiusura, conteggio filtri attivi per ciascuna sezione, aggiornamento del badge sui refresh parziali) era già presente nel progetto ma priva di qualunque stile CSS (pulsante, badge e pannello collassabile risultavano invisibili/non funzionanti visivamente). Aggiunto lo stile mancante: nessuna modifica alla logica di filtraggio (AND tra filtri, risultati), come richiesto.

## v2.3-022 — 2026-07-02
- Nuova sezione **Report**, voce di menu aggiunta dopo Presenze e prima di Impostazioni. Vista principale: selettore portiere (obbligatorio), ambito temporale a due modalità alternative — "Per Stagione" (dropdown stagioni, titolo+date) o "Periodo libero" (coppia data-dal/data-al nello stesso stile affiancato già stabilito per i range di date) — e pulsante "Genera report" che calcola e mostra il risultato senza ricaricare la pagina.
- Contenuto del report, tutto calcolato solo sugli Impegni del portiere nell'ambito scelto: (A) percentuale presenze totale, scomposizione per Allenamento/Partita/Torneo (tipi senza impegni omessi), contatori Presenti/Assenti/Giustificati/Da registrare; (B) grafico andamento temporale a barre (SVG nativo, nessuna dipendenza esterna essendo Chart.js non disponibile offline nel progetto — scelta esplicitamente prevista dal prompt come alternativa), aggregazione settimanale sotto i 3 mesi di periodo, mensile oltre; (C) numero di Sedute effettuate e Top 10 esercizi più ricorrenti (stesso principio di aggregazione di "Esercizi effettuati"), con link "Vedi elenco completo" che apre la sezione accordion già esistente nella scheda portiere con lo stesso filtro di periodo già applicato; (D) Qualità Allenate più frequenti (prime 8-10) e Periodi di Allenamento coperti come badge.
- **Nessuna nuova query in storage.js**: il report riusa integralmente `_gkAttendanceEntries` (già costruito per Storico presenze) e `storage.getCompletedSessionsForGoalkeeper` (già costruito per Storico sedute/Esercizi effettuati), aggregandoli diversamente in ui.js — storage.js non è stato toccato.
- Export PDF: pulsante "Esporta PDF" che riusa la pipeline di stampa già esistente (`window.print()` + `css/print.css`, nuove regole additive senza toccare quelle della scheda esercizio); il grafico andamento è SVG inline quindi si stampa nativamente, nessuna conversione canvas→immagine necessaria. Intestazione stampabile con nome portiere, periodo e data di generazione.
- Stati vuoti: nessun portiere selezionato → invito a selezionarne uno; portiere senza impegni nel periodo → messaggio chiaro, nessuna sezione vuota renderizzata; periodo con un solo impegno → report generato regolarmente, grafico con una singola barra senza errori.
- Scheda portiere: nuovo pulsante "Genera report" tra Esporta e Modifica; naviga a Report con il portiere già preselezionato (l'ambito temporale resta da scegliere).
- Nota di revisione: nello zip caricato ho trovato alcune modifiche manuali pregresse — il pannello filtri di Presenze ha una struttura HTML con i campi Tipo/Stagione/Stato annidati dentro un unico blocco `.filter-field-wide` invece di essere celle separate della griglia (probabilmente un residuo di una modifica non completata), e la griglia filtri generale è stata ricalibrata (`minmax(100px,1fr)`, `align-items:start`). Per rispettare il vincolo "non toccare Presenze" di questo intervento non li ho modificati: funzionano (gli id sono preservati) ma visivamente i filtri Tipo/Stagione/Stato di Presenze appariranno impilati in un'unica colonna invece che affiancati. Fammi sapere se vuoi che li sistemi.

## v2.3-021 — 2026-07-02
- **Fix centratura scheda portiere**: la regola `max-width:1100px` introdotta per la scheda portiere e il profilo (viewport ≥1400px) mancava di `margin:0 auto`, quindi il blocco si restringeva ma restava ancorato al margine sinistro invece di centrarsi come le altre schede. Aggiunto `margin-left/right:auto`: ora la scheda portiere è centrata e allargata esattamente come Esercizi, Sedute e Presenze.
- **Riordino e uniformità dei filtri Presenze e di tutti i pannelli filtri**: nessuna modifica funzionale, solo aspetto/layout.
  - Nuova variabile `--ctrl-h: 42px` e altezza/border-radius uniformi (`var(--r-md)`, linguaggio "arrotondato generoso" già usato nei campi data) applicati a TUTTI i controlli filtro a riga singola: `select`, campi testo/data, campo ricerca (`.search`), tag picker (`.tp-control`), toggle di stato (`.seg`/`.seg-btn`) — riusando le classi comuni già consolidate nell'audit precedente, senza introdurre nuove varianti. Le textarea restano escluse ed invariate (selettore `input.input, select.input`, che per costruzione non le comprende). I `select` normalizzati con `appearance:none` e freccina personalizzata coerente con quella già usata altrove nell'app.
  - Griglia filtri (`.filters-pickers`/`.gk-filters-row`/`.acc-filters`) calibrata a `repeat(auto-fit, minmax(200px,1fr))` con gap 16px: i filtri di Presenze si dispongono ora su più righe leggibili invece che schiacciati su una riga sola, mantenendo `align-items:end` così i controlli semplici restano allineati in basso rispetto al picker portieri, naturalmente più alto.
  - Il picker portieri (contenuto variabile con pill) ha una nuova classe `.filter-field-wide` che gli fa occupare due colonne in larghezza invece di forzare la propria altezza sui controlli semplici della stessa riga; ricalibrato anche il fallback responsive sotto i 900px.
  - Il separatore "→" della coppia data-dal/data-al è ora centrato rispetto alla nuova altezza fissa dei campi (in precedenza calibrato con un padding fisso pensato per l'altezza precedente).

## v2.3-020 — 2026-07-02
- Audit e uniformazione estetica di TUTTI i pannelli filtri (solo layout/aspetto, nessuna modifica funzionale: logica AND/OR, combinazioni e risultati invariati). Inconsistenze corrette: (1) i controlli dello stesso tipo (dropdown, tag picker, campo data, ricerca) ora condividono un unico standard di altezza, padding, border-radius, font-size ed etichette in ogni sezione — consolidati tre stili di pannello divergenti (`.filters-pickers`/`.filter-field`, `.gk-filters-row`/`.gk-filter-field` a gap 5px, `.acc-filters` flex) in una sola griglia coerente (`repeat(auto-fit, minmax(210px,1fr))`, gap 16px, `align-items:end`); le vecchie classi restano come alias mappati sullo standard. (2) Nuovo helper `_dateRangeHtml`: ogni intervallo di date (Presenze, Storico presenze, Storico sedute, Esercizi effettuati, Attività stagione, Eventi specifici) è ora una coppia affiancata e visivamente unita "dal → al" con separatore freccia, non più due campi sparsi nella griglia. (3) Ordine dei filtri reso coerente (ricerca/soggetto → categorie → intervallo date → stato). (4) Contatore risultati e "Cancella filtri" uniformati nel piede del pannello (`.filters-foot`), inclusa la sezione Portieri prima disallineata. (5) Gap orizzontali/verticali uniformi e ritorno a capo ordinato su viewport strette (sotto 900px la griglia passa a una colonna e il range date resta comunque affiancato al proprio interno).
- Uso dello spazio su schermi grandi: su viewport ≥1400px il contenitore principale passa da `max-width:1200px` a `min(88vw, 1560px)`, riducendo i margini laterali eccessivi; sotto 1400px il comportamento responsive resta invariato. Le viste testuali (dettaglio portiere, profilo) mantengono una larghezza interna più contenuta (`max-width:1100px`) per non compromettere la leggibilità, mentre elenchi e griglie sfruttano tutta l'ampiezza.

## v2.3-019 — 2026-07-02
- **Filtro Stagione** (selezione singola, "Tutte le stagioni" come default, in AND con gli altri filtri) aggiunto ovunque esista un elenco di Impegni/Presenze: vista principale Presenze, sezione "Storico presenze" e sezione "Storico sedute" nella scheda portiere.
- **Dati orfani e "seduta fatta" corretta**: nuova `storage.getCompletedSessionsForGoalkeeper(goalkeeperId, filters?)`, calcolo SEMPRE a runtime da Impegni + linkedItems + Attendance (mai un dato salvato staticamente). Una Seduta risulta "fatta" solo se esiste un Impegno che la collega, il portiere è nei suoi goalkeeperIds e la sua Attendance su quell'Impegno ha status esattamente "present". Se l'Impegno viene eliminato o l'Attendance cambia, la seduta sparisce automaticamente dallo storico, senza pulizia manuale. Sostituita la vecchia logica di "Storico sedute"/"Esercizi assegnati" nella scheda portiere, che si basava sul campo deprecato `session.goalkeeperIds` e restava disallineata dal modello Impegno/Attendance.
- **Scheda portiere ristrutturata**: "Storico sedute", "Storico presenze" ed "Esercizi assegnati" (rinominata "Esercizi effettuati") sono ora sezioni accordion chiuse di default, con intestazione sempre visibile (titolo + contatore) e chevron animato; il contenuto (righe + filtri) viene calcolato e renderizzato solo alla prima apertura di ciascuna sezione (lazy loading). Storico sedute: filtri data/Stagione/tipo Impegno/ricerca titolo, una riga per ogni volta effettuata. Storico presenze: stessi filtri della vista principale Presenze (tipo/Stagione/data/stato) più i contatori Presenze/Assenze/Giustificate. Esercizi effettuati: aggregati per esercizio (titolo, data più recente, badge "× N volte" se ripetuto, riga espandibile con tutte le date e la seduta specifica di ciascuna, link di navigazione), filtri per ricerca titolo, Gesti Tecnici, Qualità Allenate, Periodo di Allenamento e intervallo date (qualunque occorrenza nel range).

## v2.3-018 — 2026-07-02
- Nuova sezione **Presenze**, sostituito il placeholder "In arrivo": elenco di tutti gli Impegni (GenericEvent) ordinati per data, con badge tipo coerente col calendario Stagione, indicatore "X/Y registrati" e badge di stato colorato (grigio=nessuna presenza, blu tenue=parziale, verde=tutti presenti, giallo=registrazione completa con assenze giustificate, rosso=registrazione completa con assenze non giustificate). Filtri per tipo, portiere (picker con ricerca dedicato), intervallo di date e stato registrazione (Non registrato/Parziale/Completo/Tutti), con stato vuoto quando nessun impegno corrisponde.
- Registro presenze del singolo Impegno: intestazione con tipo/data ed eventuali Sedute/Eventi collegati come riferimento di sola navigazione; per ogni portiere atteso (goalkeeperIds) avatar 28px, nome e tre pulsanti pill Presente/Assente/Giustificato a selezione singola con salvataggio automatico immediato su IndexedDB (via storage.js, nessun pulsante "Salva"), più campo note opzionale (abilitato dopo la prima registrazione). Azioni rapide "Segna tutti presenti" (solo sui portieri non ancora registrati, non sovrascrive gli altri) e "Azzera tutti" (con conferma, rimuove tutte le registrazioni dell'impegno).
- Scheda portiere: nuova sezione "Storico presenze" con elenco cronologico di tutti gli Impegni in cui il portiere è o era atteso (inclusi quelli con registrazione già esistente anche se nel frattempo rimosso dai portieri attesi dell'impegno), stato "Da registrare" per gli impegni non ancora segnati, filtro per intervallo di date e contatori "Presenze — Assenze — Giustificate" sul periodo filtrato (o su tutta la storia). Click su una riga apre direttamente il registro presenze di quell'impegno.
- Nessuna modifica a storage.js: i metodi Attendance/genericEventId, getAttendancesByGenericEvent e getAttendancesByGoalkeeper erano già pronti; l'export/import globale in Impostazioni includeva già le Attendance con genericEventId valorizzato (non toccato).

## v2.3-017 — 2026-07-02
- Rigenerazione stagione, gestione impegni dei giorni disattivati dal template: quando "Genera settimane" viene rilanciata dopo aver disattivato un giorno prima attivo, gli impegni generati per quel giorno (nel range di date della stagione, non in override) vengono gestiti su tre livelli — vuoti/senza collegamenti: eliminati subito, silenziosamente, nessuna conferma; con sedute/eventi collegati: prima di eliminare compare una modale con conteggio esatto ("interessa X impegni futuri, Y con collegamenti che verranno persi") e i pulsanti "Elimina comunque"/"Annulla rigenerazione" (annullare non applica alcuna modifica); impegni modificati manualmente (isOverride:true) non vengono mai toccati automaticamente e restano nel calendario, eliminabili solo a mano con la "×" esistente.
- Calendario Stagione, inversione della logica di click sulla card giorno: per i giorni con impegno già presente, il click sull'etichetta impegno (es. "● Allenamento 3") ora apre direttamente la MODIFICA dell'impegno; il click ovunque altro nella card (area vuota, numero giorno, pulsante dedicato) apre invece direttamente il pannello "+ Collega elemento" (tab Sedute/Eventi). Per i giorni senza alcun impegno il comportamento resta invariato: click ovunque nella cella apre sempre la creazione dell'impegno. Click su card Evento o su Seduta collegata invariati (stopPropagation, aprono editor evento/naviga a Sedute).

## v2.3-016 — 2026-06-30
- Collega elemento: ogni tab ora interroga SOLO il proprio store (Sedute → `sessions`, Eventi → `specificEvents`) tramite storage.js. Il cambio tab azzera la lista e ricarica dati diversi: la scheda Sedute mostra solo sedute, la scheda Eventi solo eventi, mai liste miste.
- Vista mese: griglia resa definitivamente uniforme forzando via stile inline `!important` il contenitore (`display:grid; grid-template-columns:repeat(7,1fr); width:100%; box-sizing:border-box; flex:1 1 100%; min-width:0`) e ogni cella (`width:100% !important; min-width:0; min-height:100px; box-sizing:border-box; overflow:hidden`), con wrapper forzato a `display:block; overflow:hidden`. Tutti i giorni sono rettangoli identici (1/7), a prova di override CSS.
- Click distinto nel calendario: click sull'etichetta impegno ("● Allenamento 3") apre DIRETTAMENTE il pannello "+ Collega elemento" (con stopPropagation); click sul resto della cella (numero, area vuota, note) apre la modifica/creazione impegno; click su card Evento apre l'editor evento; click su Seduta naviga a Sedute. L'etichetta ha ora cursor:pointer e hover (sottolineatura + leggera opacità).
- Generazione date esatte: gli impegni vengono creati iterando giorno per giorno da startDate a endDate inclusi, creando un impegno solo se quel giorno della settimana è attivo nel template. Nessun giorno prima dell'inizio o dopo la fine stagione (niente più lunedì precedenti o domeniche successive fuori range).
- Rigenerazione non distruttiva: gli impegni non modificati vengono aggiornati (solo tipo e portieri dal template) conservando i `linkedItems` (sedute/eventi collegati non vengono più persi); gli impegni modificati restano intatti; solo i giorni nuovi nel range vengono creati da zero.

## v2.3-015 — 2026-06-30
- Nomenclatura: nell'interfaccia "evento generico" diventa "Impegno" e "evento specifico" diventa "Evento" (chiavi IndexedDB, campi schema e nomi variabili invariati). Aggiornati tab, titoli, pulsanti, messaggi di conferma/errore e toast (es. "impegni pianificati", "Impegno salvato", "Impegno del …", tab "Eventi").
- Vista mese: griglia resa rigidamente uniforme in larghezza — contenitore `.calendar-month-grid` con `grid-template-columns: repeat(7,1fr); width:100%; box-sizing:border-box`; celle `.calendar-month-cell` con `width:100%; box-sizing:border-box; overflow:hidden; min-height:100px; min-width:0`; wrapper con `overflow:hidden; width:100%`. Tutti i giorni sono ora rettangoli identici (1/7 della griglia) a prescindere dal contenuto.
- Visuale calendario ridisegnata: l'Impegno è un'etichetta leggera ("● Allenamento 3", testo colorato per tipo, niente sfondo/bordo, 0.72rem/500; numero portieri solo se >0; se modificato un discreto "○", mai la scritta "Modificato"). Gli Eventi collegati sono card prominenti con sfondo/bordo/titolo; le Sedute sono testo corsivo grigio "→ titolo".
- Interazione click: click su cella o etichetta impegno → apre il pannello impegno (creazione se assente, modifica pre-compilata se presente); click su card Evento → apre l'editor di quell'Evento; click su Seduta → naviga a Sedute. stopPropagation su eventi e sedute per non aprire anche il pannello impegno.
- Pannello "+ Collega elemento" a tab [Sedute] [Eventi]: ricerca testuale + lista risultati (seduta con durata MM:SS; evento con badge tipo, titolo e data), click per collegare; "Crea nuova seduta"/"Crea nuovo evento" chiudono e reindirizzano alle rispettive sezioni; nessun form inline; lista con scroll interno.
- Il badge "Modificato" non compare più: sostituito ovunque dal simbolo discreto. I modali hanno scroll interno (`max-height:90vh`) e non vanno più in overflow.

## v2.3-014 — 2026-06-30
- Fix vista mese: griglia rigida a celle uniformi (`.calendar-month-grid` con `grid-template-columns: repeat(7,1fr)` e `grid-auto-rows: minmax(100px,auto)`; celle `.calendar-month-cell` con `min-height:100px`, `overflow:hidden`, `min-width:0`, `box-sizing:border-box`). Nessuna dimensione dipende più dal contenuto: tutti i giorni sono rettangoli identici come nella vista settimana.
- Separazione dei flussi giorno in due step non contestuali: cliccando un giorno si apre la vista giorno (o, se vuoto, direttamente il form). Step 1 = pannello dedicato al solo evento generico (tipo, portieri con picker, note); Step 2 = sezione "Elementi collegati" accessibile solo dopo il salvataggio, con "+ Collega elemento" che apre un secondo pannello separato (Seduta / Evento specifico + creazione al volo). I modali ora hanno `max-height:90vh` con scroll interno: niente più overflow fuori schermo.
- "Imposta come Override" sostituito da "Modifica evento", disponibile su tutti gli eventi generici: riapre il form in modifica. Al salvataggio, se la stagione è ciclica il flag interno viene impostato automaticamente e la pill nel calendario si aggiorna subito.
- Eliminazione evento generico: "×" sulla pill in vista giorno, settimana, mese e lista attività. Stagione libera → conferma semplice. Stagione ciclica su giorno del pattern → modale con tre opzioni: "Elimina solo questo evento", "Elimina questo e tutti i successivi dello stesso giorno" (stessa stagione, stesso giorno della settimana, data ≥ corrente), "Annulla".
- Linguaggio utente ripulito dai termini tecnici: "Modifica metadati" → "Modifica stagione"; tooltip card ora specifici ("Modifica esercizio/portiere/seduta/evento"); il badge "Override" diventa "Modificato"; nessuna occorrenza visibile di "metadati"/"Override"/"isOverride".

## v2.3-013 — 2026-06-30
- Interfaccia Stagione completa sul modello dati 2.3 (GenericEvent/SpecificEvent). Il calendario ora è guidato dagli eventi generici indicizzati per data (un evento generico per giorno), non più da settimane con giorni annidati.
- Form stagione: titolo, date, toggle "Rendi ciclica?" (isCyclic). Se ciclica, template microciclo per-giorno con toggle giorno attivo, dropdown tipo giorno (Allenamento/Partita/Torneo/Riposo/Altro) e picker portieri di default per giorno.
- "Genera settimane": crea i GenericEvent dall'inizio alla fine replicando il template e propagando i portieri di default del giorno; conferma se esistono già eventi; gli eventi in override non vengono sovrascritti.
- Viste calendario: Settimana (navigazione + salto a data), Mese (griglia 7 colonne a celle uniformi di altezza fissa — fix richiesto: nessuna cella dipendente dal contenuto; max 2 elementi + "+X altri"; giorni fuori mese in grigio non interattivi; click → pannello giorno), Solo giorni con attività (lista cronologica filtrabile per intervallo).
- Pannello giorno ridisegnato senza tab: Sezione 1 evento generico (tipo, portieri con picker ricerca+categoria+stato attivo e avatar, etichetta adattiva Partecipanti/Convocati, note, Imposta come Override / Ripristina template) e Sezione 2 elementi collegati (linkedItems: sedute + eventi specifici, con rimozione e "+ Collega elemento" a due colonne affiancate, inclusa creazione al volo di un evento specifico).
- Modifica massiva portieri su periodo (data inizio/fine + tipo evento opzionale, picker rimuovi/aggiungi, anteprima "X eventi", conferma) tramite storage.updateGenericEventsGoalkeeperIds.
- Lista stagioni: card con badge Ciclica/Libera, numero eventi pianificati, overlay modifica/elimina (rimuove anche i GenericEvent della stagione)/esporta. Export stagione include i GenericEvent e gli SpecificEvent collegati; import con risoluzione id duplicato (sovrascrivi/copia).
- Tab "Eventi specifici": lista con filtri tipo/data, card con overlay, editor (tipo/titolo/data/avversario/luogo/orario/note), export/import singolo. La rimozione di un evento specifico lo scollega dai GenericEvent che lo referenziano.
- Sedute: rimosso dal form il campo "Portieri coinvolti" (goalkeeperIds deprecato in B1; il dato resta nello store, non più editabile dall'UI).

## v2.3-012 — 2026-06-30
- Solo data-layer (nessuna modifica UI): schemaVersion portato a "2.3", DB IndexedDB a v3 (upgrade additivo, nessun dato toccato).
- Nuovo store `genericEvents` + entità GenericEvent (seasonId/weekId/date/eventType training|match|tournament|rest|other/goalkeeperIds/linkedItems[session|specific_event]/notes/isOverride) con metodi storage: getGenericEvents(filtri), getGenericEvent, saveGenericEvent, deleteGenericEvent, getGenericEventsByDateRange, getGenericEventsForSession (cerca in linkedItems), updateGenericEventsGoalkeeperIds (modifica massiva atomica su range date), bulkPutGenericEvents.
- Nuovo store `specificEvents` + entità SpecificEvent (eventType match|tournament|test|other/title/date/opponent/location/time/notes, senza goalkeeperIds) con metodi getSpecificEvents/getSpecificEvent/saveSpecificEvent/deleteSpecificEvent/bulkPutSpecificEvents.
- Season (additivo): aggiunto `isCyclic` (deprecato `mode`, mantenuto), `cyclicTemplate.days` con struttura per-giorno {dayOfWeek, active, eventType, defaultGoalkeeperIds} derivata da weekPattern, e `weeks[].isManuallyAdded` (specchio di manual). Migrazione idempotente: mode free→isCyclic false, cyclic/mixed→true; isCyclic già presente non viene toccato.
- Attendance (additivo): aggiunto `genericEventId` (nuovo campo principale); occasionType/occasionId deprecati ma conservati. Nuovi metodi getAttendancesByGenericEvent e getAttendancesByGoalkeeper (filtro date via evento generico collegato).
- Session.goalkeeperIds deprecato (mantenuto, non usato dalla nuova logica; il legame portiere-seduta si deriva da GenericEvent.linkedItems). Nessuna modifica al form Seduta.
- Export/import: export globale esteso a genericEvents/specificEvents; export/import singolo per GenericEvent e SpecificEvent (con rifiuto di file multipli); import difensivo con fallback neutri; pipeline import migra i file v2.2 (Season→isCyclic + days, Attendance→genericEventId) prima del salvataggio.

## v2.2-011 — 2026-06-30
- Stagione, unificazione modalità: rimosse le tre modalità Libera/Ciclica/Mista dall'interfaccia; ora un solo toggle "Rendi ciclica questa stagione?" (default off → mode "free"; on → mode "cyclic"). Le stagioni salvate come "mixed" vengono trattate come "cyclic" senza errori (migrazione additiva in normalizeSeason).
- Template microciclo per TIPO GIORNO: ogni giorno attivo del template ha solo un tipo (Allenamento/Partita/Torneo/Riposo/Altro, fissi e non configurabili), non più un periodo o una seduta specifica. "Genera settimane" crea segnaposto visivi (indicatori di tipo giorno), senza creare sedute o eventi reali. Rimossi dal template i dropdown periodo/seduta.
- Schema Event: aggiunto "training" come eventType valido (additivo, fallback neutro per valori sconosciuti).
- Override settimana migliorato: in override si possono rimuovere i segnaposto generati (× accanto a ciascuno); nuovo pulsante "Ripristina template" che riporta la settimana allo stato generato cancellando le modifiche manuali (con conferma); nuovo pulsante "Rimuovi settimana" visibile solo sulle settimane aggiunte manualmente (flag `manual`, con conferma). La rigenerazione ora preserva sia le settimane in override sia quelle manuali (anche fuori dal periodo).
- Viste calendario: nuovo selettore Settimana / Mese / Solo giorni con attività. Vista Mese: griglia 7×6 con navigazione mese, fino a 3 pill per cella + "+X altri", giorni fuori mese in grigio non cliccabili, click su cella → popover di dettaglio del giorno (elementi cliccabili, "Apri settimana", "+ Aggiungi"). Vista attività: lista cronologica dei soli giorni con almeno un elemento, con filtro Tutti / Per mese / Intervallo di date.
- Struttura settimana (additiva): il giorno passa da `trainingPeriod` a `dayType` (segnaposto) affiancato da `sessionIds`/`eventIds` reali; aggiunto il flag `manual`. Nuovo modulo helper aggiornato (`seasonLogic.js`).

## v2.2-010 — 2026-06-30
- Fix barra di navigazione (mobile): le voci del drawer non erano cliccabili perché lo scrim (fratello del `.topbar` sticky, che crea un contesto di impilamento) veniva dipinto sopra il drawer. Spostato lo scrim dentro l'header: ora condivide il contesto del topbar, il drawer (z-index 50 > scrim 45) resta sopra ed è cliccabile, e il tap su una voce naviga e chiude il drawer.
- Fix barra di navigazione (desktop): al cambio tab la barra si spostava perché la scrollbar verticale appariva/scompariva variando la larghezza del viewport. Riservato sempre lo spazio della scrollbar (`scrollbar-gutter: stable`), così la larghezza resta costante e la barra non si ridimensiona. Le voci attive cambiano solo aspetto (sfondo/colore/ombra), mai dimensioni.
- Propagazione rinomina categorie/liste: rinominando una voce in una lista configurabile ora la modifica si propaga automaticamente. Blocco dei duplicati (confronto case-insensitive dopo trim) con errore inline "Il nome '…' esiste già in questa lista"; se il nome è nuovo, la voce viene sostituita nella stessa posizione in tutti gli esercizi (technicalGestures/trainedQualities/trainingPeriod) e portieri (categoria e tag note tecnici/mentali/medici), in modo atomico (transazione IndexedDB via cursore), con `updatedAt` aggiornato e toast "Voce aggiornata in X esercizi/portieri" (solo se X>0). Materiali e frecce sono referenziati per key e già risolti a runtime: nessuna propagazione necessaria (solo il blocco duplicati sul nome del materiale). L'eliminazione di una voce resta invariata (gli esercizi la mostrano come "fuori lista").

## v2.2-009 — 2026-06-30
- Sezione **Stagione** completa (sostituisce il placeholder): lista stagioni a card (badge Libera/Ciclica/Mista, date, n. settimane) con overlay modifica/elimina/esporta; form metadati con modalità e template microciclo per-giorno; vista calendario navigabile settimana per settimana (prev/next + salto rapido) con layout a 7 giorni, sedute (titolo + durata) ed eventi (badge tipo) per giorno, "+" per assegnare, note settimana inline.
- Modalità **Libera** (aggiunta manuale delle settimane) come fondamento; modalità **Ciclica/Mista** con "Genera settimane" che replica il template dal periodo scelto (conferma di rigenerazione) e **Override settimana** per modificare una singola settimana senza toccare template né le altre.
- **Eventi** (partita/torneo/test/altro) come entità autonome: tab Eventi dentro Stagione con filtri per tipo e intervallo date, card con overlay; form con tipo, data, avversario, **portieri convocati** (picker dai portieri attivi con avatar) e note; assegnazione a un giorno dal "+" del calendario (anche creazione al volo con ritorno e assegnazione automatica).
- Struttura settimana: mantenuti i campi fissi `assignedSessionIds`/`assignedEventIds` come unione canonica + mappa additiva per-giorno `days` (sempre sincronizzate; preservate in export/import). Nuovo modulo `seasonLogic.js` (logica pura: date, generazione/rigenerazione settimane).
- Export/import: singola stagione (con eventi collegati) e singolo evento, con risoluzione id duplicato (sovrascrivi / copia); verificata l'inclusione di seasons/events nell'export globale. Riferimenti a sedute/eventi eliminati mostrati come "(seduta rimossa)" / "(evento rimosso)" senza rompere la pianificazione.

## v2.2-008 — 2026-06-30
- Fondamenta dati schema v2.2: nuovi store IndexedDB `goalkeepers`, `seasons`, `events`, `attendances` (DB_VERSION 1→2, upgrade additivo che preserva i dati esistenti). Migrazione automatica e additiva: i nuovi store partono vuoti per le installazioni esistenti.
- Nuove liste configurabili: `goalkeeperCategories`, `technicalNoteTags`, `mentalNoteTags`, `medicalNoteTags` (default scritti solo al primissimo avvio assoluto; su installazioni esistenti partono vuote, popolabili dalle Impostazioni).
- Export/import esteso: l'export globale include portieri, stagioni, eventi e presenze; l'import instrada e normalizza i nuovi tipi con le stesse regole difensive (campi mancanti → fallback neutri, sconosciuti → ignorati). Aggiunto export/import del singolo portiere con gestione id duplicato (sovrascrivi / copia).
- Sezione **Portieri** completa (sostituisce il placeholder): indice a card con foto/iniziali, categoria, età calcolata e badge Attivo/Non attivo; ricerca per nome, filtro categoria e filtro stato; overlay modifica/elimina; form con foto, anagrafica, piede preferito, altezza, stato attivo e tre blocchi note (tecnico/mentale/medico) con tag picker + testo libero; scheda dettaglio con note, storico sedute, esercizi assegnati (derivati) ed esporta singolo.
- Sedute: nuovo campo "Portieri coinvolti" (`goalkeeperIds`) nel composer, con picker dropdown + lista scelti; i portieri eliminati compaiono come "(portiere rimosso)" senza rompere la seduta (stesso pattern dei materiali).
- Impostazioni: nuove sezioni "Categorie Portiere" e "Note Portiere · Tecniche/Mentali/Mediche" (stesso pattern delle liste editabili esistenti).
- Allineamenti documentazione (README) ai nuovi store/entità.

## v2.2-007 — 2026-06-30
- Barra di navigazione: le tab del menu (Esercizi, Sedute, Portieri, Stagione, Presenze, Impostazioni) sono state spostate a destra e raggruppate accanto al blocco account con un piccolo gap (20px); il logo resta ancorato a sinistra e lo spazio vuoto espandibile è ora al centro tra logo e gruppo menu+account.
- Blocco account: la riga sotto il nome mostra ora **solo la squadra/e** (con "+X altre" se più di una). Rimosso il ruolo dalla visualizzazione compatta nella barra; il campo "ruolo" resta presente e modificabile nel form del profilo. Se non ci sono squadre la seconda riga non viene renderizzata.
- Allineamento etichette di schema a **v2.2** in commenti e stringhe (nota backup in Impostazioni, intestazioni di `importExport.js`, voci del README). Nessun cambiamento strutturale: il passaggio 2.1 → 2.2 è solo un bump di etichetta (l'export già scriveva `schemaVersion: "2.2"`).
- Introdotta la regola di versionamento dei pacchetti di consegna e creato questo CHANGELOG.

---

_Nota: le consegne precedenti a questa voce risalgono a sessioni anteriori all'introduzione del CHANGELOG e non sono tracciate qui._
